import React, { useState } from 'react';
import { arrow_down, competitions, home, live, logo, market, org, rating, ru } from '@/assets';


function Header() {
  const pages = [
    {title: 'home', icon: home},
    {title: 'market', icon: market},
    {title: 'rating', icon: rating},
    {title: 'competitions', icon: competitions},
    {title:'org', icon: org},
    {title: 'live', icon: live}
  ];

  return <div></div>
}

export default Header
